// Safe defaults keep simulator builds and CI credential-free.
GOOGLE_CLIENT_ID =
GOOGLE_SERVER_CLIENT_ID =
GOOGLE_REVERSED_CLIENT_ID =

// Optional local override. Never commit GoogleSignIn.local.xcconfig.
#include? "GoogleSignIn.local.xcconfig"
